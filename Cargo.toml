[workspace]
members  = ["programs/*"]
resolver = "2"

[profile.release]
overflow-checks = true
lto             = "fat"
codegen-units   = 1
opt-level = 3       # 强制最高等级优化，编译器会更努力地压缩栈帧

[profile.release.build-override]
opt-level     = 3
incremental   = false
codegen-units = 1

