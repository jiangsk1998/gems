pub const CONFIG_SEED: &[u8] = b"config";

pub const CLAIM_RECORD_SEED: &[u8] = b"claim_record";

pub const DEFAULT_COOLDOWN_SECONDS: i64 = 24 * 3600; // 默认冷却时间为24小时

pub const DEFAULT_AMOUNT_PER_CLAIM: u64 = 1000000; // 默认每次领取1个代币，假设代币有6位小数
