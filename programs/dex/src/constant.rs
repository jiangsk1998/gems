pub const POOL_SEED: &[u8] = b"pool";

pub const DEFAULT_FEE_NUMERATOR: u64 = 3; // 默认手续费分子

pub const DEFAULT_FEE_DENOMINATOR: u64 = 1000; // 默认手续费分母，表示0.3%的手续费

pub const MINIMUM_LIQUIDITY: u64 = 1000; // 默认最小流动性，防止池子被抽干
